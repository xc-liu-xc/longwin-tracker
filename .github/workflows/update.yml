name: Update LongWin Data

on:
  schedule:
    - cron: '0 2 * * *'  # UTC 02:00 = 北京时间 10:00
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests tushare pandas

      - name: Fetch signals
        env:
          QIEMAN_TOKEN: ${{ secrets.QIEMAN_TOKEN }}
        run: python scripts/fetch_signals.py

      - name: Fetch NAV data
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: python scripts/fetch_nav.py

      - name: Fetch content metadata
        run: python scripts/fetch_content.py

      - name: Fetch WeChat articles
        run: python scripts/fetch_wx_articles.py

      - name: Fetch posts
        env:
          QIEMAN_TOKEN: ${{ secrets.QIEMAN_TOKEN }}
        run: python -u scripts/fetch_posts.py

      - name: Install markdownify
        run: pip install markdownify -q

      - name: Export posts to markdown
        run: python scripts/export_posts_md.py

      - name: Build aggregated funds metadata
        env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: python scripts/build_funds_json.py

      - name: Analyze strategy
        run: python scripts/analyze_strategy.py

      - name: Run strategy framework predictions
        run: python scripts/predict_signals.py

      - name: Build asset allocation snapshot + timeseries
        run: python scripts/build_allocation.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data.json docs/data_s.json docs/nav/ docs/content.json docs/posts/ docs/posts-index.json docs/funds.json docs/analysis.json docs/analysis_s.json docs/strategy.json docs/strategy_s.json docs/allocation.json posts/
          git diff --staged --quiet || git commit -m "chore: update longwin data $(TZ='Asia/Shanghai' date '+%Y-%m-%d')"
          git push
